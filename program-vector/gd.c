// C++ program to sort a vector in non-decreasing
// order.
#include <iostream>
#include<vector>
using namespace std;

int main()
{
    vector<int> v;
    int size=v.size()
    v.push_back(x);

    sort(v.begin(), v.end());


    for (auto x : v)
        cout << x << " ";

    return 0;
}
